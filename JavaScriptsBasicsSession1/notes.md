# JavaScript
- To add Js
Can be:
    - Inline
    - Internal
    - External

1. Variable in JavScript
    - var :These variable can be updated can be updated and re-declared
    ```Example: var x=10 var x=20```

    - let :These can be updated but no re-declaration.

    ```Example: Let x=10 let x=20 (Invalid)```
    - const:No updation and no Re-declaration